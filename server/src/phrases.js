//var mysql = require('mysql');
//var config = require('../config/mainConfig').config;
//var async = require('async');

module.exports.phrases = function (params, callback) {
    console.log(params);
    return callback('err');
};